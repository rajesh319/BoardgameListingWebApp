pipeline {
    agent any
    
    tools {
        maven 'maven3'
        jdk 'jdk17'
    }
           
        stage('Build') {
            steps {
                sh "mvn package"
            }
        }
    }
}
